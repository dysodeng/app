# 应用配置
app:
  name: "app"
  env: "dev"
  domain: "http://localhost:8080"
  jwt:
    secret:

# 服务配置
server:
  http:
    enabled: true
    port: 8080
  grpc:
    enabled: true
    port: 3000
  websocket:
    enabled: true
    port: 4000
  task:
    enabled: true
  cron:
    enabled: true
  health:
    enabled: true
    port: 5000
